events {
    worker_connections 1024;
}

http {
    server {
        listen 8080;

        # API routes - proxy to backend
        location /api/ {
            proxy_pass http://http-backend:3000;
        }

        # Frontend routes - proxy to Astro server
        location / {
            proxy_pass http://frontend:4321;
        }
    }
}
